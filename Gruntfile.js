'use strict';

module.exports = function (grunt) {
	// define configuration to all tasks
	grunt.initConfig({});

	// load grunt plugins
	grunt.loadNpmTasks();

	// define tasks
	grunt.registerTask('default');
};